import assert from 'assert';
class Person {
    name: string = "";
    set personName(a: string) {
        this.name = a;
    }
}
let p = new Person();
p.personName = "name";
assert(p.name === "name");
