import assert from "assert";
interface e2 {
    h2: number;
    i2(para: number): number;
}
let f2 = class C7 implements e2 {
    h2: number = 7;
    i2(h2: number): number {
        return h2 * 2 * this.h2;
    }
};
let g2 = new f2();
assert(g2.h2 === 7);
assert(g2.i2(2) === 28);
